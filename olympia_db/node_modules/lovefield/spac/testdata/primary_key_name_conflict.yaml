%YAML 1.2
---
name: sample
version: 1
table:
  Employee:
    column:
      id: string
      name: string
      pkEmployee: string  # Should throw: conflicts with primary key name
    constraint:
      primaryKey: [id]
