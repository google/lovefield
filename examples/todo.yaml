%YAML 1.2
---
name: todo
version: 1
table:
  Item:
    column:
      id: integer
      description: string
      deadline: datetime
      done: boolean
    constraint:
      primaryKey: [ id ]
    index:
      idx_deadline:
        column: [ deadline ]
        order: desc
