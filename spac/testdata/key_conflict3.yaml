%YAML 1.2
---
name: sample
version: 1
table:
  Employee:
    column:
      id: string
      name: string
    constraint:
      primaryKey: [id]
      unique:
        unqId:
          column: [id]  # Should throw: the column is already unique
